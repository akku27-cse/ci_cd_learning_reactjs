import React from 'react';

const Header = () => {
  return (
    <header className="app-header">
      <h1>Minimal Resume Builder</h1>
      <p>Create a professional resume in minutes — no sign up required</p>
    </header>
  );
};

export default Header;